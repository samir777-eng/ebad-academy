# Database
DATABASE_URL="postgresql://user:password@localhost:5432/ebad_academy?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# File Storage (Cloudflare R2 or AWS S3)
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
R2_PUBLIC_URL=""

# Cron Job Security (REQUIRED in production)
# Generate with: openssl rand -base64 32
CRON_SECRET="your-cron-secret-here"

# Vercel KV (REQUIRED in production for rate limiting)
# These are automatically set by Vercel when you create a KV database
# For local development, rate limiting will use in-memory storage
KV_REST_API_URL=""
KV_REST_API_TOKEN=""
KV_REST_API_READ_ONLY_TOKEN=""

# Sentry Error Tracking (OPTIONAL - for production error monitoring)
# Get your DSN from https://sentry.io after creating a project
# The wizard has already configured this in sentry.*.config.ts files
# SENTRY_DSN is automatically read from those config files
# SENTRY_AUTH_TOKEN is only needed for source map uploading (stored in .env.sentry-build-plugin)
# SENTRY_ORG="your-sentry-organization"
# SENTRY_PROJECT="your-sentry-project"

