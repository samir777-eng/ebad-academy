# Vercel KV Visual Setup Guide

## 🎯 What You'll Create

```
┌─────────────────────────────────────────────────────────┐
│                    Vercel Dashboard                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Your Project                                            │
│  ├── Deployments                                         │
│  ├── Storage                                             │
│  │   ├── Postgres (ebad-academy-db) ✅                  │
│  │   └── KV (ebad-academy-kv) ← YOU'LL CREATE THIS     │
│  └── Settings                                            │
│      └── Environment Variables                           │
│          ├── DATABASE_URL ✅                             │
│          ├── KV_REST_API_URL ← AUTO-ADDED              │
│          ├── KV_REST_API_TOKEN ← AUTO-ADDED            │
│          └── KV_REST_API_READ_ONLY_TOKEN ← AUTO-ADDED  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 📸 Step-by-Step Screenshots Guide

### Step 1: Navigate to Storage Tab

```
Vercel Dashboard → Your Project → [Storage] Tab
                                    ^^^^^^^^
                                    Click here
```

You'll see:
```
┌────────────────────────────────────────┐
│  Storage                                │
├────────────────────────────────────────┤
│                                         │
│  [Create Database]  ← Click this       │
│                                         │
│  Existing Databases:                   │
│  • ebad-academy-db (Postgres) ✅       │
│                                         │
└────────────────────────────────────────┘
```

### Step 2: Select KV Database Type

```
┌────────────────────────────────────────┐
│  Create Database                        │
├────────────────────────────────────────┤
│                                         │
│  Choose a database type:               │
│                                         │
│  ○ Postgres                            │
│  ○ Blob                                │
│  ● KV  ← Select this                   │
│  ○ Edge Config                         │
│                                         │
│  [Continue]                            │
│                                         │
└────────────────────────────────────────┘
```

### Step 3: Configure Database

```
┌────────────────────────────────────────┐
│  Create KV Database                     │
├────────────────────────────────────────┤
│                                         │
│  Database Name:                        │
│  [ebad-academy-kv]  ← Enter this       │
│                                         │
│  Region:                               │
│  [iad1 - Washington, D.C., USA ▼]     │
│   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^     │
│   Choose same as Postgres              │
│                                         │
│  [Create]  ← Click when ready          │
│                                         │
└────────────────────────────────────────┘
```

### Step 4: Connect to Project

After creation, you'll see:

```
┌────────────────────────────────────────┐
│  ebad-academy-kv                        │
├────────────────────────────────────────┤
│                                         │
│  Status: Active ✅                      │
│  Region: iad1                          │
│  Created: Just now                     │
│                                         │
│  [Connect Project]  ← Click this       │
│                                         │
└────────────────────────────────────────┘
```

Then select environments:

```
┌────────────────────────────────────────┐
│  Connect to Project                     │
├────────────────────────────────────────┤
│                                         │
│  Project:                              │
│  [ebad-academy ▼]  ← Select yours      │
│                                         │
│  Environments:                         │
│  ☑ Production  ← Check this            │
│  ☑ Preview     ← Check this (optional) │
│  ☐ Development ← Leave unchecked       │
│                                         │
│  [Connect]  ← Click to finish          │
│                                         │
└────────────────────────────────────────┘
```

### Step 5: Verify Environment Variables

```
Settings → Environment Variables

┌────────────────────────────────────────────────────────┐
│  Environment Variables                                  │
├────────────────────────────────────────────────────────┤
│                                                         │
│  KV_REST_API_URL                                       │
│  https://xxx.kv.vercel-storage.com                     │
│  Production, Preview                                   │
│                                                         │
│  KV_REST_API_TOKEN                                     │
│  ••••••••••••••••••••••••••                           │
│  Production, Preview                                   │
│                                                         │
│  KV_REST_API_READ_ONLY_TOKEN                          │
│  ••••••••••••••••••••••••••                           │
│  Production, Preview                                   │
│                                                         │
└────────────────────────────────────────────────────────┘
```

## 🔄 How It Works

### Architecture Diagram

```
┌─────────────────────────────────────────────────────────┐
│                     User Request                         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Vercel Edge Network                         │
│              (Your Application)                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ├──────────────┬──────────────┐
                     ▼              ▼              ▼
            ┌────────────┐  ┌────────────┐  ┌────────────┐
            │  Postgres  │  │  Vercel KV │  │  Storage   │
            │  Database  │  │  (Redis)   │  │  (R2/S3)   │
            │            │  │            │  │            │
            │  User Data │  │ Rate Limit │  │   Files    │
            │  Progress  │  │   Cache    │  │   Images   │
            │  Lessons   │  │  Sessions  │  │   Videos   │
            └────────────┘  └────────────┘  └────────────┘
```

### Rate Limiting Flow

```
1. User makes request
   │
   ▼
2. Middleware checks rate limit
   │
   ├─→ Check Vercel KV for request count
   │   │
   │   ├─→ Count < Limit? → Allow request ✅
   │   │                     Increment counter
   │   │
   │   └─→ Count ≥ Limit? → Block request ❌
   │                         Return 429 error
   │
   ▼
3. Request processed (if allowed)
```

## 📊 What Gets Stored in KV

```
Key Pattern: rate-limit:{identifier}:{endpoint}

Examples:
┌──────────────────────────────────────────────────────┐
│ Key                              │ Value  │ TTL      │
├──────────────────────────────────┼────────┼──────────┤
│ rate-limit:192.168.1.1:/signin   │ 3      │ 900s     │
│ rate-limit:user@email.com:/admin │ 7      │ 3600s    │
│ rate-limit:192.168.1.2:/api/quiz │ 15     │ 60s      │
└──────────────────────────────────┴────────┴──────────┘

TTL = Time To Live (auto-expires)
```

## ✅ Success Indicators

After setup, you should see:

1. **In Vercel Dashboard:**
   - ✅ KV database shows "Active" status
   - ✅ Connected to your project
   - ✅ 3 environment variables added

2. **In Deployment Logs:**
   ```
   ✓ KV connection successful
   ✓ Rate limiting enabled
   ✓ Using Vercel KV for production
   ```

3. **In KV Metrics:**
   - ✅ Requests counter increasing
   - ✅ Storage showing data
   - ✅ No connection errors

## 🎓 Quick Reference

| Action | Location | Time |
|--------|----------|------|
| Create KV | Storage → Create Database | 2 min |
| Connect | KV Dashboard → Connect Project | 1 min |
| Verify | Settings → Environment Variables | 1 min |
| Redeploy | Deployments → Redeploy | 3 min |
| Test | Use curl commands | 2 min |

**Total Time:** ~10 minutes

---

**Next:** [Test your setup](./VERCEL_KV_SETUP.md#step-7-test-rate-limiting)

